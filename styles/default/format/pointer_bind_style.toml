[policy]
name = "pointer_bind_style"
enabled = false
type = "python"
touch_contract = "code_only" # any | code_only | preprocessor_only | whitespace_only

# options: bind_to_type | bind_to_name
style = "bind_to_type"
